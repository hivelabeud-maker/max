# 전체 워크플로우 다이어그램

프로젝트 고유명사 없이 6단계 게이트 흐름만 표시한다. 실제 프로젝트의 진행 상태는 해당 프로젝트 폴더의 `00-note.md` 요약 섹션에 기록한다.

```mermaid
flowchart TD
    G1["Gate 1<br/>SB 분석<br/>장면별 12항목 표 + 공간이동경로·감정곡선<br/>(프롬프트 작성 금지)"] --> G2
    G2["Gate 2<br/>Visual Bible<br/>세계관·형태문법·재질·색상·시간대·깊이규칙<br/>(프롬프트 작성 금지)"] --> G3
    G3["Gate 3<br/>Shot Matrix<br/>숏별 12항목 + 이미지제작순서≠영상편집순서"] --> G4a

    subgraph GATE4["Gate 4 · 구조 마스터 제작"]
        G4a["4-a 씬 생성<br/>기본: 레퍼런스정리→프롬프트설계→1샷생성<br/>(여기서 처음 프롬프트 작성)"] -->|승인| G4b
        G4a -.요청 시만.-> G4a3["3안 비교<br/>A 충실형 · B 확장형 · C 극대화형"] -.하나 승인.-> G4b
        G4b["4-b Structure Master 잠금"] --> G4c
        G4c["4-c Scene Gap Analysis<br/>6개 공백 카테고리<br/>필수/권장/선택"]
    end

    G4c --> G5

    G5["Gate 5<br/>파생과 부분 수정<br/>6개 분류 중 하나로 먼저 분류<br/>(한 패스 한 변수만)"] --> G6

    G6["Gate 6<br/>최종 출력<br/>Magnific 업스케일 · Higgsfield 영상화"] --> QC
    QC["Deliver<br/>QC & Archive<br/>9개 관찰가능 품질조건 검사<br/>원본/마스터/편집/업스케일 버전 보관"]

    G5 -.반려·재작업.-> REV["revision-protocol<br/>어느 Gate로 돌아갈지 판정"]
    REV -.-> G2
    REV -.-> G4b
    REV -.-> G5
```

## 게이트별 정지 규칙

- Gate 1, 2는 이미지 프롬프트 작성 자체를 금지한다.
- Gate 4-a에서 처음으로 프롬프트를 쓰되, 캐릭터·FX 없는 무인 배경까지만이다. 기본은 1샷이고, 3안 비교는 사용자가 명시적으로 요청할 때만 한다.
- Gate 4-b(Structure Master 잠금)는 반드시 Gate 4-a에서 하나의 안이 승인된 뒤에만 시작한다.
- Gate 4-c(Scene Gap Analysis)는 Gate 4-b 이후에만 시작하고, 이 단계에서는 추가 씬의 목록과 우선순위만 승인받는다. 장문 프롬프트는 Gate 5에서 쓴다.
- Gate 5는 요청마다 먼저 6개 분류 중 하나로 분류하고, 그 한 가지 작업만 처리한다.
- 매 응답은 현재 Gate 번호와 목적을 한 줄로 표시하고 시작한다.
- 각 게이트·하위 단계는 사용자 승인이 있어야 다음으로 넘어간다. 화살표는 "승인 후 이동"을 뜻하지 자동 진행을 뜻하지 않는다.
