# 넷마블 플레이어블 · 360° 원형 코어 디펜스

- **클라이언트**: 넷마블
- **업무 범위**: 플레이어블 광고 1편 — 28초 페이크 게임 영상 (단일 HTML, 외부 요청 0건)
- **일정**: 2026-09-09 착수 · 납기 미확정
- **견적**: 협의중
- **상태**: v4 배포판 완료. CTA 링크·영상 추출 규격 회신 대기

## 요약 (매 세션 종료 시 갱신)

- **핵심 결정사항**
  - 장르는 **360° 원형 코어 디펜스**(뱀서라이크 A 액션·서바이버). 레퍼런스는
    Geometry Cannon : Idle Defense — 게임 구조만 가져오고 룩은 쓰지 않는다.
  - 룩은 v4에서 **우주**로 고정. 검정 `#04030A` + 보라 성운, 탄막만 바이올렛 계열.
  - 배경은 사진을 심지 않고 코드로 그린다. 용량과 외부 요청 0건을 지키기 위함.
    사진이 필요해질 때를 위해 `BG_IMAGE` 슬롯을 열어 뒀다.
  - 조작은 드래그 하나. 무입력이면 오토플레이로 28초를 완주한다.
  - 결과창에는 **CTA 하나만** 둔다. `다시 보기`는 스토어 이동과 시선을 나눠 갖는다.
  - 블루는 **#6DDBFF 한 색**. 게이지·전리품·버튼·마커가 전부 이 색조(195°)에서 파생된다.
  - 좌우 여백은 **24px 하나**(`GUT`). HUD·CTA·결과창이 전부 여기서 파생된다.
  - 네온은 **항상 두 겹**(색 무리 + 흰 심). 한 겹이면 뿌옇게 뭉친다.
  - 보스는 **기계 하우징 + 균열 난 에너지 코어**. 체력이 깎이면 균열이 늘어난다.
  - HP에 하한(170)을 걸어 **절대 지지 않는다**. 광고라서 패배 화면이 없어야 한다.
  - 교체 슬롯 3개(`CTA_URL` · `CHARACTER.frames` · `BG_IMAGE`)만 만지면 다른 타이틀로
    재사용된다.

- **결정 근거**
  - 광고 네트워크 상한(대개 5MB)에 여유를 남겨야 나중에 캐릭터 프레임을 늘릴 수 있다.
    성운 사진을 data URI로 넣으면 용량이 서너 배가 되므로 코드 드로잉을 택했다.
  - 배경과 탄막이 둘 다 보라라서 중앙에서 탄이 묻힌다. 탄두 안쪽 34%를 흰색으로
    남겨 대비를 만들었다.

- **다음 액션**
  - 넷마블 — `CTA_URL` 스토어 링크 회신
  - 넷마블 — 영상 추출 규격(1080p 여부) 확정
  - MAX실 — 실기기 프레임 확인 (지금 수치는 소프트웨어 렌더링 기준)

## 파일

| 항목 | 경로 |
|---|---|
| 현행 v4 | `netmarble-playable/index.html` |
| 인수인계 | `netmarble-playable/README.txt` |
| 발주서 | `netmarble-playable/발주서.md` |
| 이전 판 | `netmarble-playable/_이전버전/` (v1 다크코랄 · v2 도트UI · v3 네온아케이드) |

버전 히스토리와 각 판의 판단 근거는 발주서에 정리돼 있다.

## 원본 로그 (append-only, 절대 덮어쓰지 않음)

### 2026-09-09
- 1차 세션. 레퍼런스 영상 2건 + 스틸 3장에서 장르를 특정하고 v1~v3을 만들었다.
- v1 다크코랄 → v2 도트UI(방향 오독) → v3 네온 아케이드(딥 네이비 + 격자 그리드).
- `design-critique` 게이트 1회 통과 후 🔴 항목 반영(발주서 검수 이력 참조).
- 이 세션 산출물은 저장소에 올라가지 않았다. 2차 세션에서 zip으로 다시 받아 커밋했다.

### 2026-09-10
- 2차 세션. 실장님 지시 세 줄을 반영해 **v4**를 만들었다.
  1. 배경 교체 — 딥 네이비 격자 그리드 → 검정 + 보라 성운
  2. 캐릭터 30% 축소 — 표시 폭 172px → 120px
  3. 탄막을 보라 계열로 — 글로우 + 잔상
- **배경**: 참고 이미지(검정 우주 + 중앙 보라 성운 + 붉은 별 섞인 별밭)를 받았으나
  파일 원본을 세션에서 꺼낼 수 없어 같은 톤을 코드로 재현했다.
  `buildSky()` 로 한 번만 굽고, 시드 RNG를 써서 열 때마다 같은 별자리가 나온다.
  - 첫 시도는 성운 감쇠가 가팔라 원 테두리가 보였다 → 감쇠를 완만하게 깔고
    세로로 1.14배 늘려 정원(正圓)으로 보이지 않게 고쳤다.
  - 사진을 그대로 쓰고 싶을 때를 위해 `BG_IMAGE` 슬롯을 만들었다.
    값을 넣으면 구워둔 성운을 그 이미지가 덮는다(cover).
- **캐릭터**: `CHARACTER.w` 한 값만 바꿨다. 발밑 골드 링은 원래 `w`에 연동돼 있었고,
  총구 위치는 상수 58이 박혀 있어 `CHARACTER.w * 0.34`로 바꿨다.
  이걸 안 고쳤으면 축소한 캐릭터 바깥에서 탄이 튀어나왔을 것이다.
- **탄막**: 핑크 → 바이올렛(`violet` `violetL` `violetD` 3토큰으로 분리).
  잔상은 탄마다 6칸 배열 하나를 붙여 프레임당 좌표 1점을 밀어 넣는 방식.
  각성 구간에 탄이 100발 넘게 남는데, 탄마다 배열을 새로 만들면
  초당 수천 개가 생겼다 사라져 프레임이 떨어진다.
- **버린 대안**: 잔상을 고스트 3장(같은 다트를 뒤로 겹쳐 그리기)으로 만드는 방법.
  유도탄이 곡선을 그려서 직선 뒤로 겹치면 궤적이 어긋난다. 좌표 기록 방식으로 갔다.
- 검증: Chromium 헤드리스로 2·6·14·20.5·23·26·28.2초 프레임을 렌더링해 확인.
  콘솔 에러 0건, 외부 요청 0건, 용량 100KB.

### 2026-09-10 (2차 · 같은 날 추가 지시)
- 실장님 지시 두 건 반영.
  1. 결과창에서 `다시 보기` 제거
  2. 캐릭터 발밑 링을 골드 → 네온 보라로
- **다시 보기**: 문구·탭 영역(`placeHit`)·클릭 핸들러·죽은 상수 `END_AGAIN` 까지
  같이 걷어냈다. `showEnd()` 는 이제 CTA 하나만 켠다.
  `reset()` 함수 자체는 남는다 — 최초 시작과 녹화 하네스가 쓴다.
- **발밑 링**: 한 겹으로 그었더니 배경 성운과 같은 계열이라 바닥에 묻혔다.
  골드일 때는 보색이라 한 겹으로도 떴는데 보라는 안 된다.
  네온관 구조로 두 번 긋는 방식으로 바꿨다 —
  굵은 바이올렛(#8E5CF7, 11px) 헤일로 + 가는 흰빛(#EADCFF, 4px) 심.
- 아티팩트로도 게시했다 (문서 껍데기만 벗기고 게임 코드는 동일).
  https://claude.ai/code/artifact/8b1171c8-1950-443a-8291-ed0d8ef7445e
  재생성: `index.html` 에서 1~7행과 37~38행, 마지막 2행을 빼고 `<title>` 만 갈면 된다.

### 2026-09-10 (2차 · 3차 지시)
- 실장님 지시 네 건. 참고 이미지 2장(인게임 블루 버튼 / 결과창 빨간 표시) 동봉.
  1. 인게임 하단 CTA 버튼을 블루톤으로
  2. 결과창 `CLEAR` 아래 가로 구분선 제거
  3. 피해 게이지 우측 빈 여백을 줄이도록 바를 길게
  4. 결과창 버튼도 같은 블루로
- **CTA 블루**: 팔레트에서 `lime/limeL/limeD` 를 `cta/ctaL/ctaD` 로 갈았다.
  `ctaButton()` 한 함수를 인게임 하단과 결과창이 같이 쓰므로 3·4번이 한 번에 해결됐다.
  색 이름 대신 역할 이름(`cta`)으로 둔 건, 다음에 또 색이 바뀌어도
  토큰 이름이 거짓말이 되지 않게 하려는 것.
- **게이지 길이**: 고정 폭 420px이라 `27k` 같은 짧은 수치에서 80px 가까운
  빈 칸이 남았다. 숫자 폭(`tw()`)을 재서 바 길이를 역산하도록 바꿨다.
  - 결과창은 수치가 멈춘 화면이라 실측값으로 딱 맞춘다.
  - 인게임은 수치가 계속 올라서, 실측으로 재면 `9.9k → 10k` 넘어가는 순간
    바 길이가 튄다. 최대 자릿수(`999k`)를 한 번 재서 고정했다.
- **범위 밖 판단**: 실장님이 짚은 건 결과창이었지만, 인게임 하단
  `스킬 피해 현황` 패널이 같은 위젯에 같은 결함이라 같이 고쳤다.
  결과창은 1.5초, 인게임 패널은 28초 동안 보인다.
- 검증: 7개 시점 재렌더링, 콘솔 에러 0건.

### 2026-09-10 (2차 · 4차 지시)
- 실장님 지시 세 건.
  1. 하단 CTA 버튼 등장이 느리다
  2. CTA 블루톤을 #6CDBFF 로 지정
  3. 보스 도형이 꽃문양 같다 — 균열 난 에너지 구체나 기계 코어로
- **CTA 등장**: `T.PILL` 3.0초 → 1.2초, 페이드 0.6초 → 0.22초.
  "등장 속도"는 페이드를 가리키지만 등장 시점도 같이 당겼다.
  28초 광고에서 CTA가 화면에 있는 시간이 1.8초 늘어난다.
  되돌릴 거면 `T.PILL` 한 값이다.
- **CTA 색**: #6CDBFF 를 hsl(195°, 100%, 71%) 로 읽고 밝기만 바꿔 3토큰을 만들었다.
  `cta` #6CDBFF(71%) · `ctaL` #B3ECFF(85%) · `ctaD` #00B8F5(48%).
  색조가 같아서 버튼 전체가 지정색 하나로 읽힌다.
- **보스**: 곡선 꽃잎 8장을 방사 배치한 게 꽃으로 보인 원인이었다.
  꽃잎을 각진 도넛 조각으로 바꾸고, 층마다 회전 속도를 다르게 줬다.
  - 바깥 장갑판 6장(r 54~76, `+rot`) — 어두운 금속 + 시안 테두리
  - 안쪽 구동링 3장(r 41~48, `-rot × 1.6`) — 바이올렛
  - 에너지 코어(r ~34, 회전 없음) — 껍데기 안에 갇힌 것으로 보이게
  부품이 같은 속도로 돌면 장식, 따로 돌아야 기계다.
- **균열**: 등장 시 시드 RNG로 6줄을 뽑아 두고(매 프레임 뽑으면 깜빡인다),
  체력이 깎인 비율만큼 개수를 늘려 보여 준다. 90%면 1줄, 다 깎이면 6줄.
  체력 바를 안 봐도 남은 체력이 형태로 읽힌다.
  흰 선 밑에 어두운 선을 한 겹 깔았다 — 코어 안쪽이 밝아 흰 선만으로는 묻혔다.
- 검증: 타임라인 7개 시점 + 보스 4개 시점 렌더링, 콘솔 에러 0건.

### 2026-09-10 (2차 · 5차 지시)
- 실장님 지시 네 건 + 참고 이미지 3장(결과창·인게임 빨간 표시 / 보스 도형 PNG).
  1. 버튼·좌우 하단 간격이 들쭉날쭉하다 — 그리드에 맞춰라
  2. CTA 블루를 #3BEFE0 네온톤으로
  3. 전리품 아이콘이 뿌옇다 — 네온톤을 밝게
  4. 보스 에너지 코어 퀄이 나쁘다 (참고 PNG 첨부)
- **그리드**: 한 화면에 여백이 24 / 40 / 54 세 값 섞여 있었다.
  24(인게임 HUD 다수) · 40(CTA 버튼) · 54(결과창 패널·게이지).
  인게임에서 가장 많이 쓰던 24로 통일하고 `GUT` 상수 하나로 묶었다.
  `CONT_L`(24) · `CONT_R`(696) · `CONT_W`(672)가 여기서 파생.
  설정 아이콘도 오른쪽 선(696)에 맞췄다.
  → 눈으로 맞추면 화면마다 다시 어긋난다. 한 값에서 뽑아야 안 어긋난다.
- **CTA 색**: #3BEFE0 = hsl(175°, 85%, 58%). 색조 고정, 밝기만 바꿔 3토큰.
  `cta` #3BEFE0(58%) · `ctaL` #8EF6ED(76%) · `ctaD` #0FB3A6(38%).
- **글리프 뿌옇던 원인**: 네온을 한 겹으로 그리고 있었다. 같은 색으로 획을 긋고
  같은 색으로 8px 번지게 하니 획이 자기 무리에 먹혀 경계가 사라졌다.
  캐릭터 발밑 링에서 쓴 2겹 방식(색 무리 + 흰 심)을 `glyph()` 에 옮겼다.
  도형 코드를 클로저로 감싸 두 번 호출하는 구조 — 도형 정의는 그대로 뒀다.
  `lighten()` 헬퍼 추가(rgba 문자열로 들어오면 그대로 반환).
- **보스 코어**: 평평한 방사 그라디언트라 원판으로 보였다.
  봉인 링 2줄(서로 다른 축으로 회전) + 왼쪽 위 스페큘러 하이라이트를 얹었다.
  하이라이트 하나가 납작한 원을 구슬로 바꾼다.
  ⚠️ **첨부하신 보스 도형 PNG가 흰 이미지로만 들어와 열리지 않았다.**
  방향을 못 봐서 자체 판단으로 개선했다. 원본 재전달 필요.
- 압축파일은 이제 요청 시에만 만든다 (실장님 지시).
- 검증: 타임라인 7개 시점 + 보스 4개 시점, 콘솔 에러 0건.

### 2026-09-10 (2차 · 6차 지시)
- "블루톤이 잘 안 잡힌다. 게이지·전리품·버튼 블루는 다 #6DDBFF 로."
- **원인**: 블루가 네 갈래로 흩어져 있었다.
  팔레트 시안 #6FE3FF / CTA 별색 #3BEFE0 / 경험치 바 전용 #1B7FB8 /
  rgba(111,227,255) 하드코딩 5곳(점선 궤도·연쇄 번개·파티클·글자 무리).
  값이 조금씩 달라서 "같은 블루인데 안 맞는" 상태가 됐다.
  사람 눈은 절대값보다 차이를 먼저 읽는다 — 둘이 나란히 있으면 둘 다 틀려 보인다.
- **처리**: 기준색 #6DDBFF = hsl(195°,100%,71%) 하나를 정하고
  여섯 토큰을 전부 이 색조에서 밝기만 바꿔 뽑았다.
  `cyan` #6DDBFF(71%) · `cyanD` #15A6D5(46%) · `ice` #D6F5FF(92%)
  `cta` #6DDBFF(71%) · `ctaL` #B3ECFF(85%) · `ctaD` #00B8F5(48%)
  하드코딩 5곳도 rgba(109,219,255) 로 통일, 경험치 바는 `P.cyanD` 를 쓰게 했다.
- 앞선 5차의 #3BEFE0(민트 계열)은 폐기. 블루 계열이 아니라 갈라져 보였다.
- 검증: 타임라인 7개 시점, 콘솔 에러 0건. 남은 옛 블루 리터럴 0건(grep 확인).

### 2026-09-10 (2차 · 7차 지시)
- 레퍼런스(Geometry Cannon 스크린샷) 기준으로 세 가지를 맞췄다.
  1. **궤도** — 레퍼런스는 점선 원 하나. 이쪽은 셋이 겹쳐 있었다
     (바깥 퍼플 실선 RING+48 · 시안 점선 RING · 안쪽 흐린 원 148).
     점선 하나만 남기고 연보라 rgba(201,164,255)로. 손 가이드 호도 같은 색으로.
  2. **적 3종** — 전부 같은 마름모에 색만 달라 물량 구간에서 한 덩어리로 보였다.
     `ring` → 속이 빈 흰 링 / `shard` → 면 넷인 파란 보석 / `heavy` → 십자 섬광 발광체.
     형태가 다르면 색을 못 봐도 구분된다.
  3. **보스 코어** — 방사 그라디언트 구슬은 면이 없어 평평했다.
     세로로 긴 팔면체를 면 넷으로 갈라 밝기 순 배치(왼위 최명 → 오아래 그늘).
     균열은 색을 뒤집었다 — 흰 결정 위에 흰 선은 안 보인다.
     어두운 틈 + 새어 나오는 핑크 빛, 결정 실루엣으로 clip.
- 실장님이 결과창 애니메이션 보완안을 텍스트로 먼저 달라고 하셨다. 구현 보류, 제안만.
- 검증: 타임라인 7개 시점 + 보스 4개 시점, 콘솔 에러 0건.

### 2026-09-10 (2차 · 8차 지시)
- "십자 섬광이 뻗는 흰 발광체가 퀄리티가 매우 낮다" + 도트 도형 PNG 전달(이번엔 열렸다).
- 7차의 `heavy` 표현은 선 두 줄(십자) + 발광 원이 전부였다.
  형태라고 할 게 없어서 다른 이펙트와 구분이 안 됐다. 지적이 맞다.
- 전달받은 PNG 기준으로 8팔 도트 도형을 `HEAVY_CELLS` 배열(15×15 셀)로 다시 짰다.
  가운데 사각 + 직교 4팔(넓은 머리 + 가는 대) + 대각 4팔(계단 쐐기).
  90° 회전 대칭이 성립하도록 좌표를 맞췄다.
- 지킨 것 둘:
  - **회전 없음** — 도트를 각도로 돌리면 계단이 무너진다(캐릭터와 같은 원칙)
  - **정수 좌표** — 적이 움직이면 좌표가 소수라 가장자리가 흐려진다.
    이 종류만 Math.round() 로 픽셀에 앉힌다
- 무리는 셀마다 걸지 않고 그라디언트 한 장으로. 셀 25개 × 적 15마리에
  각각 그림자를 걸면 물량 구간 프레임이 깎이고 가장자리도 뭉갠다.
- 검증: 적 3종을 고정 배치해 한 프레임 렌더링(shape.js), 콘솔 에러 0건.
- 결과창 애니메이션 제안(A 순차등장 / B 게이지 차오름 / C CTA 글로우 호흡)은 회신 대기.
